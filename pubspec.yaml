name: snake_mvp
description: Snake MVP full screen, Riverpod, asset audio
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.0.3
  shared_preferences: ^2.3.2
  audioplayers: ^6.1.0

flutter:
  uses-material-design: true
  assets:
    - assets/sfx/eat.wav
    - assets/sfx/game_over.wav
